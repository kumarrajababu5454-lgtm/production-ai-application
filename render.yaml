services:
  - type: web
    name: production-ai-application
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run frontend/app.py --server.address 0.0.0.0 --server.port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
